export enum CellType {
  Buildable = "buildable",
  Path = "path",
  Spawn = "spawn",
  Goal = "goal",
}

export enum Difficulty {
  Easy = "easy",
  Normal = "normal",
  Hard = "hard",
}

export enum TargetingPriority {
  First = "first",
  Nearest = "nearest",
  Strongest = "strongest",
  Weakest = "weakest",
}

export enum TowerType {
  Arrow = "arrow",
  Cannon = "cannon",
  Frost = "frost",
  Lightning = "lightning",
  Poison = "poison",
  Sniper = "sniper",
  Tesla = "tesla",
}
